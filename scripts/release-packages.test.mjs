import{mkdtemp,mkdir,writeFile}from'node:fs/promises';import{tmpdir}from'node:os';import{join}from'node:path';import{describe,expect,it}from'vitest';import{discoverPackages,planReleases}from'./release-packages.mjs';
describe('batch package release planning',()=>{it('discovers only public zero-runtime packages in stable name order',async()=>{const root=await mkdtemp(join(tmpdir(),'toy-release-')),base=join(root,'packages');await mkdir(join(base,'b'),{recursive:true});await mkdir(join(base,'a'),{recursive:true});await mkdir(join(base,'private'),{recursive:true});await writeFile(join(base,'b','package.json'),JSON.stringify({name:'@toy/b',version:'2.0.0',dependencies:{}}));await writeFile(join(base,'a','package.json'),JSON.stringify({name:'@toy/a',version:'1.0.0'}));await writeFile(join(base,'private','package.json'),JSON.stringify({name:'private',version:'1.0.0',private:true}));expect((await discoverPackages(root)).map(x=>x.name)).toEqual(['@toy/a','@toy/b'])});it('plans only versions absent from the registry fixture',async()=>{const packages=[{name:'@toy/a',version:'1.0.0',directory:'/a'},{name:'@toy/b',version:'2.0.0',directory:'/b'}],existing=new Set(['@toy/a@1.0.0']);expect((await planReleases(packages,async(n,v)=>existing.has(`${n}@${v}`))).map(x=>x.name)).toEqual(['@toy/b'])});it('fails closed when a package gains runtime dependencies',async()=>{const root=await mkdtemp(join(tmpdir(),'toy-release-')),dir=join(root,'packages','bad');await mkdir(dir,{recursive:true});await writeFile(join(dir,'package.json'),JSON.stringify({name:'@toy/bad',version:'1.0.0',dependencies:{leftpad:'1.0.0'}}));await expect(discoverPackages(root)).rejects.toThrow('runtime dependencies must stay empty')})});
