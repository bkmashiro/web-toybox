import'../../src/toy-page.css';import{mountYoyo}from'@web-toybox/yoyo';
const stage=document.querySelector<HTMLElement>('#stage')!,sound=document.querySelector<HTMLButtonElement>('#sound-unlock')!,returnButton=document.querySelector<HTMLButtonElement>('#return')!,release=document.querySelector<HTMLButtonElement>('#release')!,reset=document.querySelector<HTMLButtonElement>('#reset')!,phase=document.querySelector<HTMLOutputElement>('#phase')!,spin=document.querySelector<HTMLOutputElement>('#spin')!,length=document.querySelector<HTMLOutputElement>('#length')!;
const yoyo=mountYoyo(stage,{sound:true});sound.addEventListener('click',async()=>{sound.textContent='正在打开…';const ok=await yoyo.unlockSound();if(ok)sound.hidden=true;else sound.textContent='再点一下打开声音'});returnButton.addEventListener('click',yoyo.return);release.addEventListener('click',yoyo.release);reset.addEventListener('click',yoyo.reset);const names={falling:'下落',sleeping:'睡眠',returning:'回线'}as const;setInterval(()=>{phase.value=names[yoyo.state.phase];spin.value=`${Math.abs(yoyo.state.disc.angularVelocity).toFixed(0)} rad/s`;length.value=`${yoyo.state.stringLength.toFixed(0)} px`},120);(window as Window&{__yoyo?:unknown}).__yoyo=yoyo;
